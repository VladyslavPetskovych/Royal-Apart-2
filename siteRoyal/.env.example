# Копіюйте в .env.local для локальної розробки (файл не комітити).
# Без цих змінних сайт використовує production origin — поведінка не змінюється.

# Корінь сайту без слешу в кінці (той самий хост, де доступний /api/...).
# VITE_SITE_ORIGIN=https://royalapart.online
